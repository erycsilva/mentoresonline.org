//-----------------------------------------
//-     SERIE COVER COMPONENT
//- 
//-     This component displays a card as a Series Cover, with information about the specific Category.
//- 
//-    RECEIVES:
//-    * serie {Object} – Receives the an object, more specifically built to receive the [CATEGORIES] Object.
//-
//-----------------------------------------

mixin serie-cover(category)
  article.serie-cover
    .serie-cover--bg(style="background-image: url("+category.bg+");")
    a.serie-cover--link(href="/series#" + category.meta, title="Acesse os vídeos da série " + category.name, rel='noopener noreferrer')
      h4.serie-cover--hidden-heading= category.name
      img.serie-cover--logo(src=category.images.logo.white, alt="Logo da série " + category.name)
      p.serie-cover--description= category.description
      button.serie-cover--button
        | Acessar
        i.serie-cover--button-icon